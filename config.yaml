# dependency-guardian configuration
server:
  listen_addr: ":8080"
  read_timeout: 30s
  write_timeout: 60s
  max_request_body: 10485760 # 10 MB

upstreams:
  npm: "https://registry.npmjs.org"
  pypi: "https://pypi.org"
  go: "https://proxy.golang.org"
  maven: "https://repo1.maven.org/maven2"

policies:
  directory: "./policies"

logging:
  level: "info" # debug, info, warn, error
  format: "text" # text, json

# Vulnerability database (OSV)
vulndb:
  enabled: true # set to true to enable
  driver: "sqlite" # "sqlite" or "postgres"
  dsn: "./vulndb.sqlite" # file path for sqlite, or postgres connection string
  # dsn: "host=localhost user=guardian password=secret dbname=guardian sslmode=disable"
  max_open_conns: 10
  max_idle_conns: 5
  conn_max_lifetime: 1h
  log_level: "warn" # silent, error, warn, info

# OSV data synchronisation
sync:
  enabled: true # set to true to enable background sync
  seed_on_start: true # run a full sync when the server starts
  ecosystems: # empty = defaults to npm, PyPI, Go, Maven
    - "npm"
    - "PyPI"
    - "Go"
    - "Maven"
  full_sync_interval: 24h # how often to do a full reseed
  delta_sync_interval: 15m # how often to check for delta updates
  metrics_interval: 1h # how often to recalculate metrics
  batch_size: 100
  workers: 2

# Dependency rewrite rules
# Rules are evaluated in order; the first matching rule wins.
# Strategies: pin, nearest-minor, nearest-major, min, max,
#             replace-major, replace-minor, replace-patch
# Modes: transparent (default, serve silently), redirect (HTTP 302)
# rewrites:
#   rules:
#     # Pin a specific package to an exact version
#     - match:
#         ecosystem: "npm"
#         name: "lodash"
#       rewrite:
#         version:
#           strategy: "pin"
#           target: "4.17.21"
#
#     # Snap all npm packages to their nearest minor release (zero out patch)
#     - match:
#         ecosystem: "npm"
#         name: "*"
#       rewrite:
#         version:
#           strategy: "nearest-minor"
#
#     # Enforce a minimum version for a PyPI package
#     - match:
#         ecosystem: "pypi"
#         name: "requests"
#       rewrite:
#         version:
#           strategy: "min"
#           target: "2.28.0"
#
#     # Cap a Go module at a maximum version
#     - match:
#         ecosystem: "go"
#         name: "github.com/example/lib"
#       rewrite:
#         version:
#           strategy: "max"
#           target: "v1.5.0"
#
#     # Replace package name (e.g., redirect to a fork)
#     - match:
#         ecosystem: "npm"
#         name: "moment"
#       rewrite:
#         name: "dayjs"
#
#     # Only rewrite versions matching a glob pattern
#     - match:
#         ecosystem: "maven"
#         name: "org.apache.commons:*"
#         version: "3.*"
#       rewrite:
#         version:
#           strategy: "nearest-major"
#         mode: "redirect"
